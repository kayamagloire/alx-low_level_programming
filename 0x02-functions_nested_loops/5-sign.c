#include "main.h"

/**
 * main - _Sign prints the sign of a number
 * @n: The int to check
 *Return: 1 and print + if n is greater than 0
 */
int print_sign(int n)
{
	if (n > 0)
	{
		putchar('+');
		return (1);
	}
		else if (n == 0)
}	
